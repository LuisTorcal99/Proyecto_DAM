<Window x:Class="Proyecto_DAM.View.DetallesAsignaturaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Proyecto_DAM.View" 
        xmlns:viewmodel="clr-namespace:Proyecto_DAM.ViewModel" 
        DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
        mc:Ignorable="d"
        Title="Detalles Asignatura" Height="450" Width="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Título de la Asignatura -->
        <Label Content="{Binding Asignatura.Nombre}" 
               FontSize="20" 
               FontWeight="Bold" 
               HorizontalAlignment="Center" 
               Margin="0,0,0,20"/>

        <!-- Lista de Eventos -->
        <GroupBox Header="Eventos Relacionados" Grid.Row="1" Margin="0,0,0,20">
            <ListView ItemsSource="{Binding Asignatura.Eventos}" 
                      SelectionMode="Single" 
                      Height="150">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Evento" DisplayMemberBinding="{Binding Nombre}" Width="200"/>
                        <GridViewColumn Header="Fecha" DisplayMemberBinding="{Binding Fecha}" Width="150"/>
                        <GridViewColumn Header="Porcentaje" DisplayMemberBinding="{Binding Porcentaje}" Width="100"/>
                        <GridViewColumn Header="Nota" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Width="80">
                                        <TextBox.Text>
                                            <Binding Path="Nota.NotaValor" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                <Binding.TargetNullValue>0</Binding.TargetNullValue>
                                                <Binding.FallbackValue>0</Binding.FallbackValue>
                                            </Binding>
                                        </TextBox.Text>
                                    </TextBox>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!-- Botones de Guardar y Borrar -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <!-- Botón Guardar -->
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Width="120" Height="40" Background="#f1cae4" BorderBrush="Transparent" FontWeight="Bold" Cursor="Hand" Margin="10,0"/>

            <!-- Botón Borrar -->
            <Button Content="Borrar" Command="{Binding BorrarCommand}" Width="120" Height="40" Background="Red" BorderBrush="Transparent" FontWeight="Bold" Cursor="Hand" Margin="10,0"/>
        </StackPanel>
    </Grid>
</Window>
